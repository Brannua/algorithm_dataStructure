/**
 * double exp(double x);
 * 求 e 的 x 次幂
 */

#include <cmath>
#include <iostream>
using namespace std;

int main() {
    double x = 1,
           y = 2;
    cout << exp(x) << endl;
    cout << exp(y) << endl;
    return 0;
}
